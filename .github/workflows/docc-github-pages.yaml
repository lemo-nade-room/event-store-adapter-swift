name: DocC GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "Sources/**"
      - "Package.swift"
      - ".swift-version"
      - ".github/workflows/docc-github-pages.yaml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      pages: read
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - id: swift-version
        run: echo "version=$(cat .swift-version)" >> "$GITHUB_OUTPUT"

      - uses: vapor/swiftly-action@v0.2.1
        with:
          toolchain: ${{ steps.swift-version.outputs.version }}

      - uses: actions/configure-pages@v6
        id: pages

      - uses: actions/cache@v6
        with:
          path: .build
          key: swift-docc-${{ runner.os }}-${{ runner.arch }}

      - name: Build combined DocC documentation
        shell: bash
        env:
          DOCS_BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: |
          swift package --allow-writing-to-directory docs \
            generate-documentation \
            --target EventStoreAdapter \
            --target EventStoreAdapterDynamoDB \
            --enable-experimental-combined-documentation \
            --disable-indexing \
            --warnings-as-errors \
            --transform-for-static-hosting \
            --hosting-base-path "${DOCS_BASE_PATH#/}" \
            --output-path docs

          cat > docs/index.html <<'HTML'
          <!doctype html>
          <html lang="en">
          <head>
            <meta charset="utf-8">
            <meta http-equiv="refresh" content="0; url=./documentation/">
            <title>EventStoreAdapter Documentation</title>
          </head>
          <body>
            <a href="./documentation/">Open the documentation</a>
          </body>
          </html>
          HTML

      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v5
        id: deployment
