name: Weekly Swift Package Update

on:
  schedule:
    - cron: '0 21 * * 6'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - run: swift package update

      - name: Detect changes
        id: changes
        run: |
          if [[ -n $(git status --porcelain Package.resolved) ]]; then
            echo "modified=true" >> "$GITHUB_OUTPUT"
          else
            echo "modified=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit & push, then create / update PR
        if: steps.changes.outputs.modified == 'true'
        env:
          BRANCH_NAME: chore/swift-package-update
        run: |
          set -e

          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git switch -C "$BRANCH_NAME"
          git add Package.resolved
          git commit -m "chore: ⬆️ Update Swift packages"
          git push --force-with-lease --set-upstream origin "$BRANCH_NAME"

          # PR が存在するかチェック
          if gh pr view "$BRANCH_NAME" --json number --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh pr edit "$BRANCH_NAME" \
              --title "📦 Update Swift packages" \
              --body "Swift パッケージを最新化しました。Package.resolved の差分をご確認ください。" \
              --add-reviewer lemo-nade-room
          else
            gh pr create \
              --base main \
              --head "$BRANCH_NAME" \
              --title "📦 Update Swift packages" \
              --body "Swift パッケージを最新化しました。Package.resolved の差分をご確認ください。" \
              --reviewer lemo-nade-room        # ★ create のときは --reviewer
          fi
